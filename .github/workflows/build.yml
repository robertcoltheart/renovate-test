name: Build

on:
  workflow_dispatch

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    container:
      image: bash:3.2.57
    
    steps:
      - name: Checkout
        uses: actions/checkout@v5
